[package]
name = "goreecloud-download-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
goreecloud-download-core = { path = "../download-core" }
goreecloud-download-fs = { path = "../download-fs" }
goreecloud-download-http = { path = "../download-http" }
goreecloud-download-state = { path = "../download-state" }
goreecloud-download-store-sqlite = { path = "../download-store-sqlite" }
reqwest = { version = "=0.13.5", default-features = false, features = ["blocking", "rustls"] }

[lints]
workspace = true
